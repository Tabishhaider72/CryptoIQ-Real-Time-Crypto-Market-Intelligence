services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner

    ports:
      - "3000:3000"

    env_file:
      - .env

    environment:
      - REDIS_URL=redis://redis:6379
      - NEXT_PUBLIC_SOCKET_URL=http://localhost:4001

    depends_on:
      redis:
        condition: service_healthy

    dns:
      - 8.8.8.8
      - 8.8.4.4

    restart: unless-stopped

  socket:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner

    ports:
      - "4001:4001"

    env_file:
      - .env

    environment:
      - REDIS_URL=redis://redis:6379

    depends_on:
      redis:
        condition: service_healthy

    command: ["sh", "-c", "pnpm socket & pnpm worker & wait"]

    restart: unless-stopped

  redis:
    image: redis:7-alpine

    ports:
      - "6379:6379"

    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

    restart: unless-stopped
